{
  "name": "agent-observatory",
  "version": "1.0.1",
  "private": true,
  "license": "MIT",
  "description": "本机 Coding Agent 会话观察台 | Local session dashboard for Codex, Claude Code, OpenClaw and Pi.",
  "repository": "github:zty-f/agent-observatory",
  "homepage": "https://github.com/zty-f/agent-observatory#readme",
  "bugs": "https://github.com/zty-f/agent-observatory/issues",
  "keywords": ["coding-agents", "codex", "claude-code", "openclaw", "pi-agent", "local-first", "session-management"],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check src/server.js && node --check public/app.js && node --test"
  }
}
