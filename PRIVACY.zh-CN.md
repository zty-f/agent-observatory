# 隐私声明

[English](PRIVACY.md) · [简体中文](PRIVACY.zh-CN.md)

更新日期：2026-09-18。本声明描述的是 **Agent Observatory 应用本身**；Codex、Claude Code、OpenClaw、Pi、GitHub 和浏览器各有自己的隐私实践。

## 应用会读取什么

运行时，本机服务会扫描用户目录下受支持 Agent 的会话文件，或你为 Pi 指定的目录。文件可能含有提示词、回答、工具调用与结果、项目路径、模型名称、时间，以及会话中出现的密钥。服务解析这些数据形成索引，并把所请求的会话数据返回给 `127.0.0.1` 上的浏览器。应用不创建远程账户，也不把会话内容上传到项目运营的服务器。

浏览器仅在 `localStorage` 保存所选 Agent、显示方式和主题。应用不设置 Cookie，不内置分析、广告、外部字体或模型 API 客户端，也不会主动向 Agent 服务商发送网络请求。各 Agent 自身的联网行为和隐私政策不在本应用控制范围内。

## 本地保存与操作

索引和对话按请求从 Agent 文件读取；观察台不另存云端副本。将会话移入废纸篓时，应用在 `~/.codex/.codex-hub-trash-index.json` 保存原路径及元数据，把会话文件移动或复制到 macOS 废纸篓；Codex 会话还可能在废纸篓保存单会话 SQLite 历史快照。恢复时会使用这些本地记录。Codex 的归档和废纸篓操作也会更新其本机原生记录。其他 Agent 可能保留自己的索引、缓存或备份；已打开的客户端也可能继续显示内存中的旧条目。

应用不会自动清理废纸篓记录；macOS 或用户也可以在应用之外移除废纸篓文件，此时可能无法恢复。从页面永久删除的是观察台跟踪的废纸篓文件，**不能保证会话的所有副本都已清除**。操作系统、备份、Agent 或同步盘仍可能持有副本。通过 `start.sh` 启动的当前用户服务，会把标准输出和错误写入 `~/.agent-observatory.log`；分享日志前请先检查内容。

如需停止读取，运行 `./stop.sh`；之后可自行移除项目目录。要清除界面偏好，请在浏览器中删除此站点的数据。Agent 会话文件和 macOS 废纸篓应分别通过对应软件及系统管理；卸载观察台不会删除 Agent 历史。

## 本机访问与第三方

服务只监听 `127.0.0.1`，检查本机 Host 和 Origin 请求头，也不开放跨域读取。它**没有登录或按用户授权机制**；本机其他进程仍可能访问这个端口。不要转发端口、放到公网代理后面，或在不可信的共享账户上使用。回环地址上的 HTTP 未使用 TLS 加密。

应用页面只加载项目自带代码与系统字体。访问 GitHub 仓库、提交 Issue 或 PR 与运行本机应用是两回事：你发布的信息会发送到 GitHub，且可能公开。不要在公开报告中放入原始会话、令牌、私人路径或未打码的截图。GitHub 如何处理这些信息，可查阅其[隐私声明](https://docs.github.com/en/site-policy/privacy-policies/github-general-privacy-statement)。

## 问题与修订

一般问题可提交[已脱敏的 GitHub Issue](https://github.com/zty-f/agent-observatory/issues/new/choose)。漏洞或敏感报告请使用 [SECURITY.md](SECURITY.md) 中的私密渠道。本声明的变更会记录在仓库中，以上日期标识当前版本。
